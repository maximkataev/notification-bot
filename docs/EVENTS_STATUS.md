# 🎭 Статус Команды /events

**ДАТА**: 17 мая 2026  
**СТАТУС**: ❌ **НЕ РАБОТАЕТ** (события не загружаются)

## Проблема

✅ Команда `/events` существует и вызывается  
✅ Логика фильтрации по субботам работает  
❌ **НО**: События не загружаются из источников

## Почему Источники Не Работают?

| Источник | Статус | Проблема |
|----------|--------|---------|
| **visitgeorgia.ge/en/events** | ❌ | 404 Not Found - URL удален/изменен |
| **tkt.ge/en/events** | ⚠️ | Next.js, события загружаются через AJAX, требует API discovery |
| **Meetup RSS** | ❌ | 404 Not Found - RSS endpoint удален |
| **mtavari.ge** | ❌ | SSL CERTIFICATE_VERIFY_FAILED |
| **arthall.ge** | ❌ | 301 Redirect на грузинскую версию |
| **Google Calendar** | ❌ | Требует public calendar URL |
| **Venue websites** | ❌ | SSL errors, нет доступа |

## Решение

Нужно найти работающие источники события для Тбилиси:

### Вариант 1: Eventbrite API
```bash
https://api.eventbriteapi.com/v3/events/search
?location.address=Tbilisi
&token=YOUR_TOKEN
```

### Вариант 2: Ticketmaster API
```bash
https://app.ticketmaster.com/discovery/v2/events
?city=Tbilisi
&apikey=YOUR_KEY
```

### Вариант 3: Lokale грузинский сайт
```bash
https://lokale.ge/en/calendar
```

### Вариант 4: Полностью переписать tkt.ge парсинг
- Найти точный API endpoint для событий
- Или использовать Puppeteer вместо Playwright
- Или найти альтернативный источник

## Текущий Код

```python
# src/workers/tbilisi_events.py
# Пытается загрузить события, но все источники недоступны
# Возвращает пустой список
```

```python
# src/bot/handlers/events_handler.py
# Команда работает, но показывает:
# "На следующую неделю в Тбилиси пока ничего не найдено 🤔"
```

## Что Работает

✅ Команда `/events` создана  
✅ Логирование работает  
✅ Логика фильтрации по выходным работает (протестирована на mock)  
✅ Playwright интегрирован  

## Что НЕ Работает

❌ Загрузка событий из внешних источников  
❌ Отображение мероприятий пользователю

## TODO

1. **КРИТИЧНО**: Найти хотя бы один работающий источник событий
2. Реализовать парсинг для этого источника
3. Протестировать команду `/events` с реальными данными

**Приоритет**: HIGH - команда не полезна без событий
